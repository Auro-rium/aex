# aex Package - v2.0

Top-level package responsibilities:
- CLI entrypoint (`aex.cli`)
- daemon runtime package (`aex.daemon`)
- integration helpers (`aex.integrations`)

Runtime entrypoints:
- daemon app: `aex.daemon.app:app`
- CLI app: `aex.cli:app`
