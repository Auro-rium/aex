# observability - v2.0

Provides:
- lightweight span timing (`start_span`, `end_span`)
- burn-rate estimation windows (`1m`, `15m`, `1h`)

Metrics endpoint composes DB counters + replay health for dashboard visibility.
