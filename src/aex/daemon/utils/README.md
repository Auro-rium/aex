# utils - v2.0

Shared runtime helpers:
- logging
- config loader
- deterministic hashing/serialization
- rate limiting
- invariants
- metrics aggregation
- supervisor dead PID cleanup
- compatibility checks

These utilities are pure support modules used across control, app, and ledger paths.
